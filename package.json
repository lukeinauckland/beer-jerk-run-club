{
  "name": "beer-jerk-run-club-site",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20.9.0"
  },
  "scripts": {
    "build": "node scripts/build.mjs",
    "build:file-preview": "npm run build && node scripts/build-file-preview.mjs",
    "test:schedule": "node scripts/test-schedule.mjs",
    "validate": "node scripts/validate.mjs",
    "report": "node scripts/report.mjs",
    "check": "npm run test:schedule && npm run build && npm run validate && npm run report",
    "preview": "node scripts/serve-preview.mjs"
  },
  "dependencies": {
    "sharp": "^0.35.4"
  }
}
